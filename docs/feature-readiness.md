# 機能稼働状況と実装準備

2026-09-05 時点の公開画面とコードを照合した一覧です。画面に表示する状態は `feature_readiness.py` を正とします。

| 優先度 | 機能・導線 | 現在の状態 | 稼働している範囲 | 次の実装 |
|---|---|---|---|---|
| 完了 | 試合カレンダー | 稼働中 | 前日・翌日移動、過去の確定結果とハンデ、当日進行、将来の開始時刻・球場、ダイヤモンド速報 | — |
| 完了 | BET入力 | 稼働中 | 新規登録、編集、削除、未確定・確定の保存、ハンデ込み損益計算、Auth0利用者別保存 | — |
| 完了 | 収支マップ | 稼働中 | 総収支、勝敗、的中率、ROI、累積収支、未確定BETの精算、Excel/CSV取込 | — |
| 完了 | 予想結果 | 稼働中 | 全NPBの固定予測、終了スコア自動照合、的中率、Brier Score、スコア誤差 | — |
| 完了 | AI詳細 | 稼働中 | 軽量分析サマリー、予測検証、直近結果、公式速報自動入力、状況・戦力補正対応の勝率シミュレーター | — |
| 完了 | 球団別詳細 | 稼働中 | 12球団のシーズン戦績、順位、次戦、先発、AI予測、直近10試合 | — |

## 稼働確認済み

- HOMEから各既存ページへの遷移
- 本日の試合一覧とAI PICK表示
- 前日・翌日・日付指定による試合カレンダー移動
- ダイヤモンド型ライブ速報でNPB公式のスコア、イニング、BSO、塁状況、投手・打者・打順を表示（15秒キャッシュ）
- 過去結果はNPB公式とハンデ情報を統合し、未確定情報で確定結果を上書きしない
- 本日のAI予測、信頼度、最上位予想
- 予想結果の表表示、検索、CSVダウンロード
- 収支マップの並び替え、グラフ操作
- Weekly P/Lは日本時間の月曜〜日曜をBET履歴から自動集計し、確定損益・勝敗・ROI・未確定額を分離表示
- BETの編集・削除、未確定BETのスコア入力と自動精算
- BET保存処理の共通化、過去レコードIDの自動補完、排他制御付きの安全なJSON更新
- Auth0の`sub`を匿名化した利用者別BET保存先とログイン・ログアウト導線
- BET履歴のExcelエクスポート、Excel/CSVインポート、重複除外追加と確認付き置換
- 全NPB予測を60秒間隔で固定保存・終了スコアと自動照合
- 12球団カードから球団別の戦績・順位・次戦・直近成績へ遷移
- AI詳細の外部ハンデ取得を短時間・キャッシュ付きにし、失敗時も画面を継続表示
- AI詳細の通常表示から旧 `app.py` の全実行を外し、高度分析を開いた時だけ読み込む
- V8の点差・ハンデ・イニング・アウト・走者補正を純粋関数として独立化し、軽量画面から操作可能にした
- AI詳細の点差・イニング・攻撃側・アウト・走者をNPB公式速報から30秒ごとに自動反映し、手動入力にも切替可能にした
- 球場・先発防御率・直近の勢い・投手相性・天候・救援陣・主力選手の補正を軽量版へ移行した
- AI詳細も当日以降の最新共有日程・予想を優先し、古い予想を本日の分析として表示しない
- AI詳細の通常画面で、NPB公式の試合終了を検知して履歴へ自動保存し、速報障害時は確認付き手動保存を利用できる

## 外部設定

- Auth0利用者別保存のコードとデプロイ処理は実装済み。ログインを有効にするには、運用者がAuth0テナントの4つの本番シークレットをGitHub Actionsへ登録する。

## 実装時の共通条件

- BETデータの保存先と更新処理は `bet_store.py` に集約済み。
- JSON更新は一時ファイルからの置換と排他制御に対応済み。
- 「編集」「削除」「精算」は安定したレコードIDと更新日時を使用する。
- 外部サイト取得が失敗しても画面全体を止めず、最終成功データと取得状態を表示する。
- 操作可能に見える要素には、実処理または明確な「実装予定」表示を持たせる。

## 今回解消した準備上の不具合

- AI詳細から旧 `app.py` を実行すると `__file__` がページ側を指し、保存先が `pages/data/` へ逸れる問題を修正した。
- AI詳細内の試合履歴・BET精算は、ルートの共有 `data/`（本番では `/app/data` ボリューム）を使用する。
